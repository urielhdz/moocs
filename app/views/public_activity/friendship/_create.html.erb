<small> <strong><%= link_to activity.owner.nombre, "profile/#{activity.owner.id}" if activity.owner %> ha agregado como amigo a <% if activity.trackable %> </strong>
	<%= link_to activity.trackable.friend.nombre, "profile/#{activity.trackable.student.id}"
 %> <%end%></small>