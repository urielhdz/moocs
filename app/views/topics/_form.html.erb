<%= form_for(topic) do |f| %>
  <% if topic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(topic.errors.count, "error") %> prohibited this topic from being saved:</h2>

      <ul>
      <% topic.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :titulo %><br>
    <%= f.text_field :titulo %>
  </div>
  <div class="field">
    <%= f.label :corte %><br>
    <%= select_tag("topic[corte]", options_for_select([['Primer corte', 1], ['Segundo Corte', 2], ['Tercer corte', 3]], 1)) %>
  </div>
  <input type='hidden' name='topic[course_id]' value='<%= course.id %>'>
  <div class="field">
    <%= f.label :descripcion %><br>
    <%= f.text_area :descripcion %>
  </div>
  <div class="actions">
    <%= f.submit "Crear curso", class: 'btn btn-info' %>
  </div>
<% end %>
